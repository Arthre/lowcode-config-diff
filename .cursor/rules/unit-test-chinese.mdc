---
description: 单元测试 describe/it 说明使用中文
globs: '**/*.test.{ts,tsx,js,jsx}'
alwaysApply: false
---

# 单测说明语言

生成或修改单元测试时：

- `describe` / `it` / `test` 的标题说明使用**中文**
- 说明写清场景与期望，例如：`key 为空时返回 undefined`
- 被测符号名（函数名、模块名）可保留英文，放在 `describe` 中即可
- 代码标识符、断言仍遵循仓库既有约定；仅标题说明强制中文

```ts
// ❌ BAD
it('returns undefined when key is empty', () => {})

// ✅ GOOD
it('key 为空时返回 undefined', () => {})
```
